<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>Holly Amos tweets all the Star Trek</title>
		<link rel="stylesheet" href="css/style.css">
	</head>
	<body>
		<div class="container m-0-auto">
			<h1 class="text-center">
				<a href="https://twitter.com/hollyamos22">@hollyamos22</a>
				tweets all the Star Trek
				<img src="images/starfleet-logo.png" alt="Delta by Chris from the Noun Project" class="logo">
			</h1>
			<table>
				<thead>
					<tr class="header-row">
						<th scope="col" class="table-header text-left">Time (U.S. Pacific)</th>
						<th scope="col" class="table-header text-left">Tweet</th>
					</tr>
					<tbody>
						{% for tweet in tweets %}
							<tr>
								<td class="p-t-s p-b-s">{{ tweet.time() }}</td>
								<td class="p-t-s p-b-s">{{ tweet.formattedText() }}</td>
							</tr>
						{% endfor %}
					</tbody>
				</thead>
			</table>
		</div>
		{{ include('footer.twig') }}
	</body>
</html>
